apiVersion: apps/v1
kind: Deployment
metadata:
  name: "romanagent"
  namespace: "agents-dev"
  labels:
    app: "romanagent"
    team: "dev"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "romanagent"
  template:
    metadata:
      labels:
        app: "romanagent"
        team: "dev"
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: "sa-romanagent"
      containers:
        - name: "romanagent"
          image: "acrshoppoc.azurecr.io/agents/romanagent:latest"
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: "config-romanagent"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
